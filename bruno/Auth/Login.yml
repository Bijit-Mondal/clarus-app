info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/v1/account/sessions/email
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "hello@bijit.xyz",
        "password": "hello@bijit.xyz"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
